# NOTE: This file is generated by chored

'on':
  workflow_dispatch: {}
  schedule:
    - cron: '0 0 * * 1,4'
jobs:
  self-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup chored
        uses: timbertson/chored-setup@v1
      - run: ./chored selfUpdate --json "$OPTS"
        env:
          OPTS: '{"mode":"pr","githubToken":"${{ secrets.GITHUB_TOKEN }}"}'
